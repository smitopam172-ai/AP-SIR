/*#include <stdio.h>

int main() {
    int num;

    printf("Enter a number (up to 5 digits): ");
    scanf("%d", &num);

    // Handle negative numbers
    if (num < 0) num = -num;

    // Nested Ternary Operators
    (num <= 9)     ? printf("The number has 1 digit.\n") :
    (num <= 99)    ? printf("The number has 2 digits.\n") :
    (num <= 999)   ? printf("The number has 3 digits.\n") :
    (num <= 9999)  ? printf("The number has 4 digits.\n") :
    (num <= 99999) ? printf("The number has 5 digits.\n") :
                     printf("The number exceeds 5 digits.\n");

    return 0;
}
}*/
